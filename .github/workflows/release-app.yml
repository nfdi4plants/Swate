name: Release Swate Electron App
description: Build and release the Swate Electron app. Crossplatform build and release for Windows, macOS, and Linux. Will be uploaded to GitHub releases.

on:
  push:
    branches:
      - main
      - feature/github-app-release

permissions:
  contents: write

jobs:
  # # --- Job 1: Run tests ---
  # test:
  #   strategy:
  #     fail-fast: false
  #     matrix:
  #       os: [ubuntu-latest, windows-latest, macos-latest]
  #   runs-on: ${{ matrix.os }}
  #   steps:
  #     # Checkout the repository
  #     - name: Checkout code
  #       uses: actions/checkout@v5

  #     # Set up .NET
  #     - name: Set up .NET
  #       uses: actions/setup-dotnet@v5
  #       with:
  #         dotnet-version: 10.x.x

  #     # Restore .NET tools
  #     - name: Restore .NET dependencies
  #       run: dotnet tool restore

  #     # Install Node.js
  #     - name: Set up Node.js
  #       uses: actions/setup-node@v6
  #       with:
  #         node-version: 22
  #         cache: "npm"

  #     # Install npm dependencies
  #     - name: Install root npm dependencies
  #       run: npm ci

  #     # Install component npm dependencies
  #     - name: Install component npm dependencies
  #       working-directory: src/Components
  #       run: npm ci

  #     # Install playwright dependencies
  #     - name: Install Playwright dependencies
  #       working-directory: src/Components
  #       run: npx playwright install --with-deps chromium

  #     - name: Run Tests
  #       run: dotnet run --project ./build/Build.fsproj test

  # --- Job 2: Create release ---
  release:
    # needs: test
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]
    runs-on: ${{ matrix.os }}
    steps:
      # Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v5

      # Set up .NET
      - name: Set up .NET
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.x.x

      # Restore .NET tools
      - name: Restore .NET dependencies
        run: dotnet tool restore

      # Install Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: "npm"

      # Install npm dependencies
      - name: Install root dependencies
        run: npm i

      # publish swate-app
      - name: Publish swate-app
        working-directory: src/Electron
        run: npm run publish
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}